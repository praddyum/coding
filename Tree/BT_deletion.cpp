/*
Use post order traversal and instead of printing the node free it

void delete_tree(node * root){
    if(root==NULL)
        return;
    delete_tree(root->right);
    delete_tree(root->left);
    free(root);
}
*/
